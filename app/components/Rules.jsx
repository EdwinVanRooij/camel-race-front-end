var React = require('react');

var Rules = React.createClass({
    render: function () {
        return (
            <div>
                <h2>Rules</h2>
            </div>
        );
    }
});

module.exports = Rules;
