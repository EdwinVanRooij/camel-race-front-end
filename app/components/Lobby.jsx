var React = require('react');

var Lobby = React.createClass({
    render: function () {
        return (
            <div>
                <h2>Lobby</h2>
            </div>
        );
    }
});

module.exports = Lobby;
